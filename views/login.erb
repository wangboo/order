
<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <title>垫江国家税务局点餐系统</title>
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta name="description" content="">
    <meta name="author" content="">

    <!-- Le styles -->
    <link href="/css/bootstrap.css" rel="stylesheet">
    <style type="text/css">
      body {
        padding-top: 40px;
        padding-bottom: 40px;
        background-color: #eee;
      }

      .form-actions {
        padding: 19px 29px 29px;
        margin: 0 auto 20px;
        background-color: #fff;
        border: 1px solid #e5e5e5;
        -webkit-border-radius: 5px;
           -moz-border-radius: 5px;
                border-radius: 5px;
        -webkit-box-shadow: 0 1px 2px rgba(0,0,0,.05);
           -moz-box-shadow: 0 1px 2px rgba(0,0,0,.05);
                box-shadow: 0 1px 2px rgba(0,0,0,.05);
      }
      .form-actions .checkbox {
        margin-bottom: 10px;
      }
      .form-actions input[type="text"],
      .form-actions input[type="password"] {
        font-size: 16px;
        height: auto;
        margin-bottom: 15px;
        padding: 7px 9px;
      }
      label {
      	font-size: 1.2em;
      }
	  .form-signin-heading {
	  	margin-left: auto; 
	  	margin-right: auto; 
	  	width: 50%;
	  	margin-bottom: 25px;
	  }
    </style>
    <link href="/css/bootstrap-responsive.css" rel="stylesheet">

    <!-- HTML5 shim, for IE6-8 support of HTML5 elements -->
    <!--[if lt IE 9]>
      <script src="../assets/js/html5shiv.js"></script>
    <![endif]-->

  </head>

  <body>

    <div class="container">
	<div class="span6 offset3">
      <form class="form-actions form-horizontal" method="post" action="/login">
        <h1 class="form-signin-heading">请登录</h1>
        <div class="control-group">
		    <label class="control-label" for="inputNum">卡号</label>
		    <div class="controls">
		      <input type="text" id="inputNum" placeholder="卡号" name="cardId">
		    </div>
   		</div>
   		<div class="control-group">
		    <label class="control-label" for="inputPwd">密码</label>
		    <div class="controls">
		      <input type="password" id="inputPwd" placeholder="密码" name="pwd">
		    </div>
   		</div>
        <input type="hidden" id="kind" name="kind" value="0"/>
        <label class="offset1 checkbox" style="margin: 5 0 5 0">
          <input type="checkbox" name="remember" value="1"> 记住我</label>
        <button class="offset2 btn btn-primary" type="submit">登录</button>
      </form>
	</div>
    </div> <!-- /container -->

    <!-- Le javascript
    ================================================== -->
    <!-- Placed at the end of the document so the pages load faster -->
    <script src="/js/jquery-1.8.3.js" type="text/javascript"></script>
    <script src="/js/bootstrap.js" type="text/javascript"></script>
	<script type="text/javascript">
		$(function(){
			$("#sel_0").click(function(){
				$("#kind").val = "0";
			});
			$("#sel_1").click(function(){
				$("#kind").val = "1";
			});
			$("#sel_2").click(function(){
				$("#kind").val = "2";
			});
		});
	</script>
  </body>
</html>
